
class ErrorStrings:
    """
    A simple class that contains well-known strings for the frontend to provide human
    readable text for.
    The attribute is pythonic snake case and the value is a network tolerant slug
    (lowercase and digits allowed and everything else converted to dashes).
    """

    invalid_credentials = "invalid-credentials"